Git Commands

git init → Initializes a new Git repository
git add . → Adds all files to staging area
git commit -m "message" → Saves changes to repository
git branch → Shows branches
git checkout branch-name → Switches branch
git push origin branch-name → Pushes code to GitHub
git pull origin branch-name → Gets latest changes